# Synchornous-FIFO-using-Verilog
A Synchronous FIFO is a queue that stores data in the order it arrives and outputs it in the same sequence. It is termed ‘synchronous’ because both read and write operations are driven by the same clock signal.
